apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: temperature-rgbmatrix-runner
  name: temperature-rgbmatrix-runner
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: temperature-rgbmatrix-runner
  template:
    metadata:
      labels:
        app: temperature-rgbmatrix-runner
    spec:
      containers:
      - name: temperature-rgbmatrix-runner
        image: ghcr.io/mmaschenk/docker-temperature-handler:1.8
        envFrom:
        - secretRef:
            name: passwords
        - configMapRef:
            name: environment
        resources:
          limits:
            memory: "100Mi"
            cpu: "50m"
        command: [ "python" ]
        args: [ "-u", "/temperature_rgbmatrix.py"]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: temperature-elasticsearch-runner
  name: temperature-elasticsearch-runner
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: temperature-elasticsearch-runner
  template:
    metadata:
      labels:
        app: temperature-elasticsearch-runner
    spec:
      containers:
      - name: temperature-elasticsearch-runner
        image: ghcr.io/mmaschenk/docker-temperature-handler:1.8
        envFrom:
        - secretRef:
            name: passwords
        - configMapRef:
            name: environment
        resources:
          limits:
            memory: "100Mi"
            cpu: "50m"
        command: [ "python" ]
        args: [ "-u", "/temperature_elasticsearch.py"]
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    app: rtl433-temperature-input
  name: rtl433-temperature-input
spec:
  replicas: 1
  serviceName: rtl433-temperature-input
  selector:
    matchLabels:
      app: rtl433-temperature-input
  template:
    metadata:
      labels:
        app: rtl433-temperature-input
    spec:
      containers:
      - name: rtl433-temperature-input
        image: ghcr.io/mmaschenk/docker-temperature-handler:1.8
        envFrom:
        - secretRef:
            name: passwords
        - configMapRef:
            name: environment
        resources:
          limits:
            memory: "100Mi"
            cpu: "50m"
        command: [ "python" ]
        args: [ "-u", "/temperature_input_rtl433.py"]
        volumeMounts:
        - mountPath: /config
          name: mappingyaml
      volumes:
      - name: mappingyaml
        configMap:
          name: temperature-input-mapping
